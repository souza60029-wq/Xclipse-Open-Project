# Tools

Tools must default to read-only behavior, print their exact environment, and emit raw output suitable for hashing. A probe can discover a fact; it does not automatically validate GPU execution. Any write, reset, command submission, or firmware interaction requires an explicit safety review and a recovery path.
